# Supplier Management System (C Project)

## 🧩 Description
This project implements a Supplier Management System using an **AVL Tree** for supplier ranking and management.  
It includes modules for:
- Supplier records and ratings  
- Inventory management  
- Order tracking  
- Searching and ranking suppliers  

---

## ⚙️ Compilation

Make sure all source files (`common.c`, `inventory.c`, `main.c`, `orders.c`, `search.c`, and `suppliers.c`) are in the same folder.

To compile:

```bash
gcc -o output common.c inventory.c main.c orders.c search.c suppliers.c
